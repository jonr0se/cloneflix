// Optional interactivity or animations
document.querySelectorAll('.row img').forEach(img => {
  img.addEventListener('click', () => {
    alert('This is a demo Netflix clone. No video available.');
  });
});
